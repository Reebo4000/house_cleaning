import 'package:flutter/material.dart';

class ProfileScreenWidet extends StatelessWidget {
  const ProfileScreenWidet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Profile Screen'),
      ),
    );
  }
}
